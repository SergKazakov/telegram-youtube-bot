# Project Guidelines

## Code Style

- Run `bunx eslint --fix && bunx tsc` after code changes
- Always use `dayjs` for date/time arithmetic (add, subtract, diffs, formatting)

## Tests

- Run `bun run test` to execute tests
